import 'package:flutter/material.dart';

class CartNews extends StatelessWidget {
  const CartNews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
